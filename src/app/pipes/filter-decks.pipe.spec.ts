import { FilterDecksPipe } from './filter-decks.pipe';

describe('FilterDecksPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDecksPipe();
    expect(pipe).toBeTruthy();
  });
});
